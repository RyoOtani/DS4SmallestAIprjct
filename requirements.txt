# tinyllm — Python training/finetuning dependencies
# Install: pip install -r requirements.txt

# Core ML
torch>=2.4.0
transformers>=4.45.0
accelerate>=0.33.0
peft>=0.12.0
bitsandbytes>=0.43.0

# Data
datasets>=3.0.0
tokenizers>=0.20.0

# Training
deepspeed>=0.15.0,<0.17.0  # ZeRO-3 + CPU offload (tested: 0.15.4)
wandb>=0.17.0      # optional, for logging

# Quantization / GGUF export
llama-cpp-python>=0.3.0
gguf>=0.10.0

# Code analysis
tree-sitter>=0.23.0  # optional, for AST analysis

# Utilities
tqdm>=4.66.0
numpy>=1.26.0
pyyaml>=6.0.0
